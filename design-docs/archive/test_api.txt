
screen 'library':
enter llm prompt: ____________ button 'send' 
no inline rag in the display...has just the send button. with just 1 input field. combo. plus there was anew cittion button at the very top.

screen 'advanced':
enter keywords:   ____________      pulldown 'input source' button1 - 'Fetch from input source'
enter llm prompt: ____________      button - call llm

screen 'Bring your own Data Chat':
enter large text plus your prompt at the end
just like the advanced chat, except there will be a bigger user entry space to see everything you are going to use
plus there will not be a keywords prompt since obviously there is no llm ever in this.... 

generally (except for the casual chat screen):
get rid of the citations button and replace with displaying all records in the dislay window inline
also get rid of the slider 'enable library'
finally get rid of the citations being appended to the llm response. the user can merely scroll above
also ensure that conversations never go away unless you hit the clear button. eventually i will add a conversation name two part pkey in conversations table
in all of these screens, the conversation in the db autopopulates the conversation window upon user entry in the screen.
add in a max-matches pulldown PLUS check if the chunk is already used and if it used then skip it.
need to add in new record types.


